// const { jsPDF } = window.jspdf;
// var doc = new jsPDF();

// const text = "This is a very long text that needs to wrap across multiple lines to fit within the width of the document.";
// const maxWidth = 60;  // Reduced maxWidth to force wrapping

// // Split the text into lines
// const splitText = doc.splitTextToSize(text, maxWidth);

// // splitText is now an array of lines
// console.log(splitText); // Will return multiple lines
// console.log(splitText.length);  // Number of lines after splitting
